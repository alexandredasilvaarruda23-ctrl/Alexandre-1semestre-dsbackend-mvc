  const userList = require("../data/users")
  
  
  const listaUsuarios = [
        {
            id: 1,
            name: "João"
        }
    ]

    const findALL = () => {
        return listaUsuarios
    }

    module.exports= {
        findALL

    } 